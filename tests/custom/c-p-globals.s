globalVar:rat := null;
anotherVar:float := 3;

main { 
  # declaration
  nullVar:int := null;
  
  # Do some stuff
  a[3] := 9999;
  
  # a var declaration that isn't at the top
  #another:int := null;
  
  # Some action
  #print 2 in [3,5,6];
  
};

anotherGlobalVar:float := 3;